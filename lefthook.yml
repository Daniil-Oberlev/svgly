pre-commit:
  parallel: true
  commands:
    oxlint:
      glob: '{*.{ts,vue},src/**/*.{ts,vue}}'
      run: yarn lint:oxlint {staged_files}
      stage_fixed: true

    eslint:
      glob: '{*.{ts,vue},src/**/*.{ts,vue}}'
      run: yarn lint:eslint {staged_files}
      stage_fixed: true

    formatting:
      glob: '{*.{ts,vue},src/**/*.{ts,vue}}'
      run: yarn format {staged_files}
      stage_fixed: true

    type-check:
      run: yarn type-check

commit-msg:
  commands:
    commitlint:
      run: yarn commitlint --edit {1}

pre-push:
  commands:
    build:
      run: yarn build

post-commit:
  commands:
    notify:
      run: echo "Коммит успешно создан!"
